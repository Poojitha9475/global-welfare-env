[project]
name = "global-welfare-env"
version = "0.1.0"
description = "Global Welfare Eligibility & Fraud Detection OpenEnv"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv==0.1.13"
]

[project.scripts]
server = "main:app"
